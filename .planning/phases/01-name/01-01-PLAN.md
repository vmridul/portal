---
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - components/features/messaging/ChatUI.tsx
  - components/features/messaging/MessageList.tsx
  - lib/utils.ts
autonomous: true
---

<tasks>
  <task id="01">
    <read_first>
      - app/layout.tsx
      - components/features/messaging/ChatUI.tsx
      - components/features/messaging/MessageList.tsx
      - lib/utils.ts
    </read_first>
    <acceptance_criteria>
      - app/layout.tsx contains <ColorProvider> wrapper
      - app/layout.tsx contains <PresenceProvider> wrapper
      - app/layout.tsx contains <ConvexClientProvider> wrapper
      - lib/utils.ts exports a function named `cn` that combines class names
      - components/features/messaging/ChatUI.tsx uses the `cn` utility for class names
      - components/features/messaging/MessageList.tsx uses the `cn` utility for class names
    </acceptance_criteria>
    <action>
      Update app/layout.tsx to wrap children with ColorProvider, PresenceProvider, and ConvexClientProvider in the correct order.
      Ensure lib/utils.ts exports the `cn` function as: export const cn = (...classes: ClassValue[]) => twMerge(clsx(classes));
      Update ChatUI.tsx and MessageList.tsx to import and use the `cn` utility for combining Tailwind classes.
    </action>
  </task>
</tasks>